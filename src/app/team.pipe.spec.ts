/* tslint:disable:no-unused-variable */

import { TestBed, async } from '@angular/core/testing';
import { TeamPipe } from './team.pipe';

describe('Pipe: Teame', () => {
  it('create an instance', () => {
    let pipe = new TeamPipe();
    expect(pipe).toBeTruthy();
  });
});
